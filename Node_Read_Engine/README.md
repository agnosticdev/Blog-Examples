# Node_Read_Engine

This project is a small web service that connects with MongoDB to serve data on authenticated routes.  The data
the web service is serving is synced data from a Drupal 7 CMS.  The Drupal project is called Drupal_Write_Engine.
